services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=auth'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=myuser'
    ports:
      - '3306'
